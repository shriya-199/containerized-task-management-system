{
  "name": "worker-service",
  "version": "1.0.0",
  "description": "Background worker service that processes pending tasks from MySQL.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "lint": "node --check server.js && node --check routes/*.js && node --check controllers/*.js && node --check models/*.js"
  },
  "dependencies": {
    "dotenv": "16.4.5",
    "express": "4.18.2",
    "helmet": "7.1.0",
    "mysql2": "3.9.7"
  },
  "devDependencies": {
    "nodemon": "3.1.4"
  },
  "engines": {
    "node": "18.20.4",
    "npm": ">=10.7.0"
  }
}
